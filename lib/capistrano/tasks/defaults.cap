namespace :load do
  task :defaults do
    set :rbenv_root,     -> { expand_home_dir('~/.rbenv', user: fetch(:user)) }
    set :rbenv_roles,    :all
    set :rbenv_map_bins, %w( bundle bundler erb gem irb rake rdoc ri ruby rails )
  end
end
